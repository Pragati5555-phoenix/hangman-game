# Hangman Game 🎮

A simple command-line Hangman game written in Python.

## Features
- Random word selection
- ASCII art hangman display
- Input validation
- Win/lose conditions

## How to Run
```bash
python hangman_soln_py3.py


---

---

## 🛠 Quick GitHub Workflow Recap
1. **Initialize Git locally**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: Hangman game"
### 2. Initialize Git Locally
Open your terminal (or Git Bash) in the project folder and run:
```bash
git init
git add .
git commit -m "Initial commit: Hangman game"
